describe('General', () => {
  beforeEach(() => cy.logout());

  it('Check if there is a color mode toggle in header.', () => { });

  it('Check if color mode could be switched to dark and back to light.', () => { });

  it('Check what happens if no mode parameter exists in local storage.', () => { });

  it('Check if color mode is being set up to dark as default.', () => { });
});
